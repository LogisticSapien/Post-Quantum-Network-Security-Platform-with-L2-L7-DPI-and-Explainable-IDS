# Prometheus scrape config for Quantum Sniffer
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'quantum-sniffer'
    static_configs:
      - targets: ['aggregator:9090']
    metrics_path: '/metrics'
